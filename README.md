# ebl
Experimental Baseball League

## Database
This project uses Postgres only. Set `DATABASE_URL` in your environment before running any scripts or the web app.

Example:
```
export DATABASE_URL="postgresql://$USER@localhost:5432/ebl"
```
